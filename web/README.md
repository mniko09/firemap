# [F] Carte Leaflet

Frontend statique (HTML/JS) consommant l'API FastAPI (`src/firemap/api/main.py`) :
`L.imageOverlay` (ou tuiles COG via titiler) pour le risque + `L.geoJSON` pour les
zones prioritaires, avec legende 4 classes. Implemente en Phase 6.
